package weighted

import "testing"

func TestWeightedAlgorithms(t *testing.T) {
	//!TODO: run all weighted balancing algorithms with random data and compare
	// the received results with expected ones (based on thir weights), while
	// allowing for some deviation percentage (based on the number of unique urls)
}
